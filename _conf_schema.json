{
    "fast_model_id": {
        "type": "string",
        "description": "记账专用轻量模型",
        "hint": "用于处理记账逻辑的轻量级模型（如 gemini-1.5-flash）。处理完后会由主模型生成最终回复。",
        "default": "",
        "_special": "select_provider"
    },
    "warning_thresholds": {
        "type": "string",
        "description": "预算预警阈值",
        "hint": "填入数字（百分比），用英文逗号分隔。例如 '20, 50, 80, 100'，当花费达到这些比例时会发出提示。",
        "default": "80, 100"
    }
}
